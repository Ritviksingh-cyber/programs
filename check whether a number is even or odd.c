#include<stdio.h>
int main()
{
    int i, j,k;
    printf("Enter the number to be checked =");
    scanf("%d",&i);
    if(i%2==0)
    {
       printf("the number is even",i);
    }
    else 
    {
        printf("the number is odd ",i);
    }
return 0;
}
