 #include <stdio.h>

int main() 
{ 
    int i , n,count;
    {
  printf("enter the number whose number of digits is to found:\n"); 
  scanf("%d",&n); 
  while (n != 0) 
  {
        n /= 10;     // n = n/10
        ++count;
    }

    printf("Number of digits: %d", count);
return 0;
}
}
