#include <stdio.h>

int main()
{
   int i , n,sum;
   printf("enter the number till which the sum is to be printed:\n");
   scanf("%d",&n);
   for(i=0;i<=n;i++)
   {
       sum=sum+i;
   }
       printf("%d",sum);
  

       return 0;
}
