
#include <stdio.h>

int main()
{
   int i , n,sum;
   printf("enter the number till which the sum is needed :\n");
   scanf("%d",&n);
   for(i=0;i<=n;i++)
   {
       if(i%2==0)
      sum=sum+i;
    }   
       printf("%d",sum);
       return 0;
       
}
